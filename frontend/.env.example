# Public app URL used for OAuth redirect and callback URLs.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional external API override. Leave empty to use same-origin Next.js API routes.
# NEXT_PUBLIC_API_URL=

# Supabase project credentials (used by Next.js /api server routes).
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Internal Go PDF service endpoint and service-to-service auth.
GO_PDF_SERVICE_URL=http://localhost:8080/api/v1/resumes/generate-pdf
GO_PDF_SERVICE_HMAC_SECRET=change-me
GO_PDF_SERVICE_ID=nextjs-api
